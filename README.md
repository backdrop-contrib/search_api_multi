# Search API: Multi-index searches

This module allows you to create Search API queries on multiple indexes that lie
on the same server.

The module was ported to Backdrop and has been merged as a submodule in the main
[Search API](https://github.com/backdrop-contrib/search_api) module.
